# PRD 346 — Attendance confirmation and no-show tracking

Owner: PRD 346 agent (resumed run — a previous agent had landed
`Backend/src/services/gameAttendance/` and was killed mid-way by a rate limit).

No heavy command was run: no `tsc`, no build, no `vitest`, no `eslint`, no `prisma generate`, no
`prisma migrate`, no backend test runner. `Backend/prisma/schema.prisma`, `prisma/migrations/`,
`package.json`, `.github/workflows/ci.yml`, `docs/UI_TEST_PLAN.md`, `docs/domains/*` and every
file on the off-limits list were **not** touched.

---

## 0. The product principle, and where it is enforced

Confirmation is a courtesy signal, never a contract. Three independent guards, deliberately
layered so that removing one still fails the build:

1. **Runtime** — `assertAttendanceUpdateIsSafe` in `attendanceRules.ts`. Every participant write
   goes through `buildAnswerUpdate` / `buildNoShowNoteUpdate` / `buildNoShowUndoUpdate`, each of
   which runs the guard against an explicit allow-list.
2. **Source scan** — `attendanceRules.test.ts` reads every file under
   `Backend/src/services/gameAttendance/` (comments stripped) and fails if any of them mentions
   `ratingUncertainty`, `reliability`, `LevelChangeEvent`, `approvedAtLevel`, `queuePosition`,
   `removeParticipant`, `leaveGame`, writes the `User` row, or performs a `gameParticipant.update`
   that does not use a guarded builder.
3. **Real database** — `gameAttendance.invariants.integration.test.ts` snapshots the whole roster,
   both `User` rows, both `UserSportProfile` rows, the `Game` row and the `LevelChangeEvent` count
   before and after **every** attendance operation and asserts that only the four attendance
   columns and the two informational counters moved.

The frontend half: `patchViewerAnswer` (the optimistic write) is isolated in a pure module and
unit-tested to prove it touches only the viewer's own row, and `AttendanceCard.test.tsx` asserts
the required caption *"Just so the organizer knows. Your seat is yours either way."* is present
in both the ask and the answered state.

---

## 1. Done

### 1.1 Backend — judgement on the inherited work

`attendanceRules.ts` (263 lines) and `attendanceCounters.service.ts` (231 lines) were good and are
kept essentially as written; they are well-factored, pure where they should be, and the
recompute-not-increment counter design is right (it makes the FINAL hook naturally idempotent).
`gameAttendance.service.ts` was sound in shape but did not compile and had two real bugs. What I
changed in it:

| Fix | Why |
|---|---|
| `projectSummary` now takes a structural `SummaryRosterRow` | the batched enricher selects fewer columns than `loadRoster`; the old signature was the `(541,54)` compile error |
| `emitSummary(gameId, userId, attendance, summary)` | **bug**: the no-show path broadcast the *organizer's* answer as if it were the noted player's. `attendanceOf()` now resolves the right player's state |
| nudge writes its system message **before** the fan-out | **bug**: the system message *is* the cooldown record; writing it after the pushes meant a push failure handed the organizer a free second nudge |
| `undoNoShow` now checks `supportsAttendance` | it was the only entry point without the guard |
| push handler uses an explicit `ATTENDANCE_ACTION_TO_ANSWER` map | the old `isAttendanceAnswer(scope.action === 'confirm' ? … : …)` was a tautology that could never fail; the map refuses a `keep`/`accept` action instead of silently treating it as `UNSURE` |
| removed the dead `RosterRow` alias, fixed the two `message` i18n keys | they pointed at keys that did not exist |

### 1.2 Backend — new

| File | What |
|---|---|
| `Backend/src/controllers/gameAttendance.controller.ts` | **new** — 7 handlers |
| `Backend/src/routes/gameAttendance.routes.ts` | filled the wave-2 stub: 7 routes, 2 rate limiters, `express-validator`, and the import that triggers the push-handler + enricher registrations |
| `Backend/src/services/push/notifications/game-no-show-noted-push.notification.ts` | **new** — the friendly heads-up, deep-linking into the game chat |
| `Backend/src/utils/systemMessages.ts` | `SystemMessageType.ATTENDANCE_NUDGED` + `USER_NOTED_NO_SHOW` + their templates |
| `Frontend/src/utils/systemMessages.ts` | the mirror enum + fallback templates (both copies changed together) |
| `Backend/src/utils/systemMessageTranslations.ts` | the two new keys in **all 11** locales |
| `Frontend/src/i18n/locales/*/chat.json` | `chat.systemMessages.ATTENDANCE_NUDGED` / `USER_NOTED_NO_SHOW` in all 11 locales |
| `Backend/src/utils/translations.ts` | 19 new keys (`attendance.*` + `errors.attendance.*`) × 11 locales |
| `Backend/src/services/push/notifications/game-reminder-push.notification.ts` | optional `{ attendanceActions }` → two signed action tokens + two localized action titles |
| `Backend/src/services/notification.service.ts` | `sendGameReminderNotification(gameId, recipients, hours, options)` — the 4th argument the inherited code already called with |
| `Backend/src/services/push/fcm.service.ts` | `nativeHandler = 'attendance_actions'` when the reminder carries an attendance token |
| `Backend/src/types/notifications.types.ts` | added `attendanceUnsureActionToken` (the scaffold had only one token field; two are needed) |
| `Backend/src/services/telegram/notifications/game-reminder.notification.ts` | `{ attendanceActions, onlyUserIds }` → two `at:` inline buttons and a filtered fan-out |
| `Backend/src/services/telegram/notification.service.ts` | passes the options through |
| `Backend/src/services/telegram/handlers/callback.handler.ts` | the `at:` branch, **immediately after the `uti:` branch** (my anchor). Edits the message to "✅ You're confirmed" / "🤔 Noted, not sure yet" and strips only the two answer buttons |
| `Backend/src/services/gameStatusScheduler.service.ts` | the **existing** 2 h reminder is now filtered to `attendance === 'UNANSWERED'` PLAYING players, and both reminders carry the actions. Also the post-commit counter refresh when the sweep moves a game to FINISHED/ARCHIVED. **No new scheduler, no deadline, no release behaviour.** |
| `Backend/src/services/results/outcomes.service.ts` | `onGameFinalizedForAttendance(gameId)` at the documented post-commit point (after `cancelAllMatchTimersForGame`, outside the transaction) |
| `Backend/src/controllers/user/stats.controller.ts` | `attendance: { rate, sampleSize, minSample } \| null` in the public projection, `null` below the ≥ 5 floor |

### 1.3 Frontend

| File | What |
|---|---|
| `Frontend/src/api/attendance.ts` | **new** — the 7 endpoints + payload types |
| `Frontend/src/api/index.ts`, `queries/queryKeys.ts` | export + the `attendance` key namespace |
| `features/attendance/attendanceVisuals.ts` | **new**, pure — dot styles (each with a text-equivalent key), `resolveDotState`, the pill percentage, the ring gauge maths, the ≥ 5 threshold, locale-aware `%` / `3/4` formatting, backend-error → own-namespace mapping |
| `features/attendance/attendancePatch.ts` | **new**, pure — the optimistic write |
| `features/attendance/useGameAttendance.ts` | **new** — query + 4 mutations + the `game-attendance-updated` socket patch |
| `features/attendance/AttendanceCard.tsx` | **new** — ask → answered morph (220 ms height/opacity, reduced-motion path), required caption, "Can't make it at all?" → the existing leave confirmation, offline hint, pending dashed-outline state |
| `features/attendance/AttendanceOrganizerStrip.tsx` | **new** — "2 of 4 confirmed" progress pill (300 ms spring, jump under reduced motion) + Nudge + cooldown caption |
| `features/attendance/AttendanceDot.tsx` | **new** — green check / grey ring / amber "?" / grey no-show, always with a `sr-only` label |
| `features/attendance/AttendanceRosterActions.tsx` | **new** — grey "No-show" tag + owner/admin overflow |
| `features/attendance/AttendanceNoShowPanel.tsx` | **new** — the after-the-game organizer roster, neutral (`tone="info"`, `confirmVariant="primary"`) confirm dialog, "Noted · Undo" toast |
| `features/attendance/AttendanceRailSummary.tsx` | **new** — mini avatar stack + "3/4", plus the structural comparator |
| `features/attendance/ShowsUpTile.tsx` | **new** — `StatTile` + a single-sky-tone ring gauge, hidden below 5 games |
| `features/attendance/AttendanceStatisticsSection.tsx` | **new** — tile + 12-month sparkline + own no-show notes with chat links (own profile only) |
| `pages/GameDetailsShell.tsx` | the card inserted **immediately after the `key="game-info"` block** (my anchor), the hook, the dot map, the legend toast, and the two `GameParticipants` props |
| `components/GameDetails/GameParticipants.tsx` | dots + no-show tag/overflow on the list rows, dot map forwarded to both carousels |
| `components/GameDetails/PlayersCarousel.tsx` | `attendanceByUserId` / `onAttendanceLegend` props, dot on the avatar, and `attendanceMapsEqual` **added to `carouselPropsEqual`** |
| `components/gameCard/GameCardRightRail.tsx` | `attendanceRail` prop, the stack, and `attendanceRailDataEqual` **added to `rightRailPropsEqual`** |
| `components/GameCard.tsx` | derives `attendanceRail` from `game.attendanceSummary` (viewer's own games only) |
| `components/LevelHistoryView.tsx` | the tile on somebody else's profile, the full statistics section on your own |
| `api/users.ts` | `UserStats.attendance` |
| `services/pushNotificationService.ts` | `GAME_NO_SHOW_NOTED` taps open the **game chat** |
| `widgets/cachedNextGameDto.ts` | the next-games envelope carries `attendance` |
| `ios/App/BandejaNextGames/.../CachedNextGame.swift` | `attendance: String?` (defaulted, so every existing call site still compiles and decoding stays backward-compatible) |
| `ios/App/BandejaWatch Watch App/Models/CachedNextGame.swift` | `needsAttendanceAnswer` (unanswered + inside 24 h + ANNOUNCED) |

### 1.4 i18n

`attendance.json` filled in **all 11 locales** with real translations. Verified with the same
three rules `Frontend/src/i18n/localeParity.test.ts` applies (identical key set, identical
placeholders, no non-`en` value byte-identical to English for values longer than 3 chars). A
separate check confirmed **every key is used and no used key is missing**.

---

## 2. Contract deltas

1. **Plural suffixes.** CONTRACT §8.3 asks for i18next `_one`/`_other` plurals where a count is
   interpolated, but `localeParity.test.ts` asserts an **exactly identical key set** across
   locales — which is incompatible with `ru` / `cs` / `sr` / `ar` needing `_few` / `_many` /
   `_zero` / `_two`. Adding only `_one`/`_other` would silently mis-resolve for those languages.
   I resolved it by **writing copy that does not need grammatical agreement**: "Nudge again in
   {{hours}} h", "Nudge sent", "{{month}} — attended {{attended}}, missed {{noShow}}". No plural
   suffixes anywhere in `attendance.json`. If the programme wants real plurals, the parity test
   needs a plural-aware key-set rule first.
2. **Two keys removed rather than allowlisted.** `stat.percent` (`"{{rate}}%"`) and
   `card.fraction` (`"{{confirmed}}/{{total}}"`) would have been byte-identical in every locale
   and failed parity. They are now formatted in code with `Intl.NumberFormat`, which is more
   correct anyway (Arabic-Indic digits, RTL percent placement). I did **not** touch
   `PARITY_IDENTICAL_ALLOWLIST`.
3. **`errors.attendance.*` stays a backend-only key space.** The `errors` i18n namespace is not
   mine, so `attendanceErrorKey()` maps each backend code onto a key in my own `attendance.errors.*`
   namespace at the call site (the same pattern `utils/genderJoinGate.ts` already uses). The
   backend keys exist in `Backend/src/utils/translations.ts` for push/Telegram surfaces.
4. **`canEditGameIncludingArchived`, not `canEditGame`.** The PRD says `canEditGame`; that
   middleware rejects ARCHIVED games, and a no-show is noted *after* the game, when it may already
   be archived. The `IncludingArchived` variant has the same role set and the same parent-game
   permission (which is what gives a league **season** owner/admin authority over a fixture).
5. **`NotificationData.attendanceUnsureActionToken` added.** The wave-2 scaffold provided one
   `attendanceActionToken`, but two shade actions need two signed tokens.
6. **No-show UI lives in the attendance card, not the roster overflow, after the game.**
   `GameParticipants` is only rendered while `resultsStatus === 'NONE'`; a FINAL game shows
   `ResultsRosterCard` instead. Rather than edit that shared component, the organizer's
   after-the-game roster (avatar + dot + tag + overflow) is rendered by `AttendanceNoShowPanel`
   inside the attendance card, which covers FINISHED, ARCHIVED and FINAL uniformly. The overflow
   *is* also present on the pre-FINAL roster rows via `GameParticipants`.
7. **Socket event name.** The PRD says `game:attendance-updated`; per CONTRACT §1/§6 the
   kebab-case `game-attendance-updated` is used.

---

## 3. Files I could not touch / orchestrator actions

1. **`package.json` test wiring** — see §5.
2. **`docs/product/constraints.md`** — please add the invariant block written out verbatim in
   `docs/plans/prd-345-357/domains/prd-346.md` §4. This is the "must never be simplified away"
   item for this PRD.
3. **Nothing else was blocked.** Shared files I did edit are all additive and were edited at the
   anchors I was given (`GameDetailsShell.tsx` after `key="game-info"`,
   `callback.handler.ts` after the `uti:` branch).

---

## 4. Known gaps

**None.** All seven are closed — 1 and 2 in §7 (the push shades), 3–7 in §8.
Each entry below keeps the original wording's subject so the history stays
readable.

1. ~~**iOS notification category is not registered in the app.**~~ The premise
   was wrong: iOS categories are registered from JS, not natively —
   `Frontend/src/services/push/registerPushNotificationActionTypes.ts` calls
   `LocalNotifications.registerActionTypes`, which ends in
   `setNotificationCategories`. That is why the grep for native symbols found
   nothing. Closed in §7.
2. ~~**Android shade actions need a Java handler.**~~ Closed in §7 with exactly
   the `AttendancePushData` / `AttendanceNotificationHelper` /
   `AttendanceActionReceiver` trio described in the original entry.
3. ~~**Watch Confirm button.**~~ The premise was wrong here too: the watch has
   its own `APIClient`, `Endpoint` enum and Keychain-backed auth (it scores live
   matches over HTTP), so no WatchConnectivity message was needed. Closed in §8.1.
4. ~~**The organizer legend is a toast, not a popover.**~~ Closed in §8.2 — and
   the iOS-Safari half of it had already been fixed by the pressable dot plus
   `AttendanceLegendButton`.
5. ~~**`attendanceSummary` on Find cards.**~~ Closed in §8.3: the rule is
   correct by construction and is now pinned by tests instead of a visual check.
6. ~~**Integration-test teardown may leak rows.**~~ Closed in §8.4.
7. ~~**No test for the reminder-filtering logic itself.**~~ Closed in §8.5.

---

## 5. Test files added

| Path | Kind | Notes |
|---|---|---|
| `Backend/src/services/gameAttendance/attendanceRules.test.ts` | backend, `node:assert`, **no DB** | pure rules + the source-scan invariant |
| `Backend/src/services/gameAttendance/gameAttendance.invariants.integration.test.ts` | backend, **needs `padelpulse_dev`** | the "assert unchanged" property, 7-day window, undo, counters, ≥ 5 threshold |
| `Frontend/src/features/attendance/attendanceVisuals.test.ts` | vitest, pure | dot text-equivalents, RTL-safe classes, ring, threshold, error mapping |
| `Frontend/src/features/attendance/AttendanceCard.test.tsx` | vitest, SSR | card states, **the required caption**, organizer strip, nudge cooldown, RTL |
| `Frontend/src/features/attendance/attendanceRoster.test.tsx` | vitest, SSR | dots, no-show tag/overflow, rail stack + comparator, optimistic patch |
| `Frontend/src/widgets/cachedNextGameDto.test.ts` | vitest, **extended** | the new `attendance` envelope field |

Suggested wiring (orchestrator owns `package.json`):

```jsonc
// Backend
"test:attendance": "ts-node --transpile-only src/services/gameAttendance/attendanceRules.test.ts",
"test:attendance-integration": "ts-node --transpile-only src/services/gameAttendance/gameAttendance.invariants.integration.test.ts",
// Frontend — fold into the PRD 345–357 vitest glob
"test:prd-346": "../scripts/run-heavy vitest run src/features/attendance src/widgets/cachedNextGameDto.test.ts"
```

Existing suites that cover files I changed and should be re-run: `test:available-games`
(enrichment), `test:auth-refresh` (push action tokens), `test:play-intent`
(`preparePushPayload` / `notificationPreference`), and whatever runs
`Backend/src/shared/sharedModuleParity.test.ts`.

**`Backend/src/services/push/pushInviteActionToken.service.test.ts` already covers the
`attendance`/`confirm` and `attendance`/`unsure` round-trip** (the wave-2 agent added it), so the
CONTRACT §10 "action token round-trip" requirement is met without a duplicate file.

---

## 6. Manual verification needed

1. **`npm run typecheck` + `npm run lint`, both lanes.** I could run neither. The riskiest spots:
   the `SummaryRosterRow` structural typing in `gameAttendance.service.ts`, the grammy
   `reply_markup` narrowing in the `at:` callback branch, and the new required `attendanceRail`
   prop on `GameCardRightRail` (only one call site, but it is a large component).
2. **A real 24 h / 2 h reminder cycle**, to confirm the 2 h message only reaches unanswered
   players and that no third message exists.
3. **Telegram on a real bot**: tap both `at:` buttons, then tap again on the edited message, then
   tap on a game you already left — none of the three may leave a spinner or throw.
4. **The nudge cooldown across a server restart** (it is read from the chat system message, so it
   should survive — this is the one place the feature deliberately does not copy the existing
   in-memory dedupe `Set`s).
5. **Light / Dark / Classic / Premium** on the attendance card, the dots and the ring gauge, plus
   `ar` mirroring of the card, the dot position (`insetInlineEnd`) and the rail stack overlap.
6. **The invariant, by hand, once**: note a no-show on a real game and confirm the player's seat,
   queue position, level, reliability and rating uncertainty are all byte-identical afterwards.
   The automated tests assert it, but this one is worth seeing.

---

## 7. Follow-up: the Android and iOS shade actions (gaps 1 and 2)

Answering from the shade now works on both platforms. Both actions are **background** actions:
they post and the app never comes forward. A failure is silent by design — there is no deadline,
so an answer that does not land simply leaves the reminder answerable.

### 7.1 Backend (two lines of payload)

`game-reminder-push.notification.ts` now also sends `attendanceConfirmedAck` /
`attendanceUnsureAck` (localized per recipient; `NotificationData` gained both fields). The shade
handlers have no i18n of their own, and `/push/invite-action` answers with a translation *key*,
not a string — so the acknowledgement has to travel with the push. Nothing else on the backend
changed: `nativeHandler = 'attendance_actions'`, both signed tokens and both button titles were
already being sent.

### 7.2 Android

| File | What |
|---|---|
| `push/AttendancePushData.java` | **new** — parses a reminder that carries *both* tokens; refuses a half-signed one. Its `notificationId()` is deliberately the same key `DataPushNotificationHelper` builds, so the 2 h reminder replaces the 24 h one |
| `push/AttendanceNotificationHelper.java` | **new** — the two buttons (titles from the payload, `R.string.attendance_confirm` / `attendance_unsure` only as fallback) and the quiet acknowledgement notification. The content tap reuses `DataPushNotificationHelper.tapExtrasMap` / `messageId`, so tapping the body behaves exactly like a reminder without actions |
| `push/AttendanceActionReceiver.java` | **new** — posts the token off the main thread via the existing `ChatReplyApiClient.performInviteAction`, then replaces the reminder with the acknowledgement. Offline or 5xx leaves the reminder alone; 4xx dismisses it |
| `push/ChatReplyMessagingService.java` | the `attendance_actions` branch, before the play-intent one. It **falls through** to the ordinary reminder when `show()` reports it could not build the two actions — a missing token must not become a missing notification |
| `push/PushIntentSanitizer.java`, `push/DataPushNotificationHelper.java` | both attendance tokens added to the launcher-poison and tap-extras skip lists, so a signed token can never be carried into a launcher Intent |
| `AndroidManifest.xml` | the receiver (not exported) |
| `res/values*/strings.xml` | `attendance_confirm` / `attendance_unsure` in **all 11** locales |

### 7.3 iOS

The category registration already had a home in JS: `registerPushNotificationActionTypes.ts`
registers every `UNNotificationCategory` through `LocalNotifications.registerActionTypes`. A
`GAME_REMINDER` category with `confirm` / `unsure` (neither `foreground`) joins the existing four
there, which is why no native registration code was needed.

| File | What |
|---|---|
| `src/services/push/pushNotificationConstants.ts` | the category id and the two action ids — they must equal the backend's signed `action` values |
| `src/services/push/registerPushNotificationActionTypes.ts` | the `GAME_REMINDER` category |
| `src/services/push/answerAttendanceFromPush.ts` | **new** — picks the token for the tapped action, posts it, schedules the localized acknowledgement, invalidates the game's attendance query |
| `src/services/pushNotificationService.ts` | the two action ids routed into it |
| `src/api/attendance.ts` | `answerFromPushToken` → `POST /push/invite-action` |
| `ios/App/App/AttendancePushData.swift` | **new** — the userInfo parser (nested `data` or flat) |
| `ios/App/App/AttendanceActionHandler.swift` | **new** — the cold-start path: when the webview is not up there is no JS to answer, so the delegate posts the token itself and shows the acknowledgement as a local notification |
| `ios/App/App/ChatReplyApiClient.swift` | `performPushAction(actionToken:)` |
| `ios/App/App/BandejaPushNotificationDelegate.swift` | the branch, right after the chat-reply one — same `jsReady` layering |
| `App.xcodeproj/project.pbxproj` | the two new files in the App target |

### 7.4 Tests

| Path | Kind | What it locks |
|---|---|---|
| `Frontend/android/app/src/test/.../AttendancePushDataTest.java` | JUnit, JVM | parsing, the half-signed refusal, the shared notification id, tokens kept out of tap extras and treated as launcher poison |
| `Frontend/src/services/push/registerPushNotificationActionTypes.test.ts` | vitest | the `GAME_REMINDER` category, the two action ids, and that **neither is `foreground`** |
| `Frontend/src/services/push/answerAttendanceFromPush.test.ts` | vitest | token per action, per-answer acknowledgement copy, query invalidation, and silence on failure |
| `Backend/.../fcm.service.test.ts` | extended | the data map the Android parser reads, and that a plain reminder gets **no** `nativeHandler` |
| `Backend/.../chat-push-reply.utils.test.ts` | extended | the reminder resolves to the `GAME_REMINDER` APNs category only when it carries actions |

Wired into the existing `test:attendance` suites on both lanes (already in CI).

### 7.5 Verified

`Backend npm run build`, `Backend npm run lint`, `Frontend npm run build`, `Frontend npm run lint`,
`npm run test:attendance` on both lanes, `Frontend/src/services/push` + `pushNotificationService`
vitest files, and `./gradlew :app:testDebugUnitTest --tests 'com.funified.bandeja.push.*'`
(JDK 21 — the default `zulu-17` on this Mac fails with `invalid source release: 21`) — all green.

The iOS App target's Swift was type-checked against the built `Capacitor.framework`
(`swiftc -typecheck`) because the full `xcodebuild -scheme App` stops earlier on a **pre-existing,
unrelated** failure: `BandejaWatchWidgets/NextGame/NextGameProvider.swift:48` uses
`WidgetRelevance`, which is iOS 18+, and that target gets compiled against the iphonesimulator SDK.

### 7.6 Still manual

The shade itself. Worth walking once per platform: both buttons with a real 24 h reminder, a cold
start (force-quit first), airplane mode, a stale token (leave the game, then tap), and a body tap.
`docs/UI_TEST_PLAN.md` §18.8b rows PN-AT-01b … PN-AT-02e.

---

## 8. Follow-up: gaps 3–7

### 8.1 The Apple Watch Confirm button (gap 3)

The original entry said the watch "has no networking layer of its own — it
renders a cached envelope". It does have one: `Services/APIClient.swift`,
`Services/Endpoint.swift` and a shared-Keychain access/refresh pair that already
carry live scoring, results and match timers. No WatchConnectivity message kind
was needed.

| File | What |
|---|---|
| `BandejaWatch Watch App/Models/WatchAttendance.swift` | **new** — `WatchAttendance.needsAnswer` (the prompt rule), the `GET` payload, the `POST` body, the answer response, and `merging()` |
| `Models/WatchGame.swift` | decodes `attendanceSummary` from `GET /games/my-games`, plus `needsAttendanceAnswer` |
| `Models/CachedNextGame.swift` | the `TODO(PRD-346)` is gone: `init(from: WatchGame)` now forwards `attendanceSummary?.viewerAttendance`, so the envelope (and the widget) finally carry a real answer instead of always `nil` |
| `Services/Endpoint.swift` | `gameAttendance` (GET) and `setGameAttendance` (POST), both `/games/:id/attendance` |
| `ViewModels/GameDetailViewModel.swift` | loads attendance alongside the game (optionally — a failure there must never hide the game) and `answerAttendance(_:)` |
| `Views/GameDetail/GameDetailView.swift` | the section: **I'm coming** / **Not sure yet**, the answered row with **Change**, the "N of M confirmed" line, and the product-principle caption |
| `Views/GameList/GameRowView.swift` | a quiet amber marker on a row that has not been answered — never a badge, never red |
| `Services/WatchCopy.swift` | eight strings in the watch's five UI languages (en/es/ru/sr/cs) |
| `App/WidgetBridgePlugin.swift` | the phone-side envelope decoder now carries `attendance` through to `CachedNextGame` |

`needsAnswer` is the 24 h prompt rule from the PRD and it requires a **future**
start, mirroring the backend's `gameAcceptsAttendanceAnswers` — the old
`hoursUntilStart` clamped at 0, which would have offered a button on a game that
had already started. The section itself is driven by the server's `answersOpen`,
so a player can also confirm early (user story 3), not only inside 24 h.

The watch targets use `PBXFileSystemSynchronizedRootGroup`, so the new files
needed no `project.pbxproj` edit.

### 8.2 The legend is a legend now (gap 4)

`AttendanceLegendSheet.tsx` (**new**) replaces the one-line `toast()` in
`GameDetailsShell`: a bottom sheet — the surface the design language prescribes
for detail — listing all four states, each next to its own dot, and repeating
"Your seat is yours either way."

The iOS-Safari half of this gap was already fixed before this round: the dot is a
real `<button>` and `AttendanceLegendButton` is a 44 px, keyboard-reachable
companion. So the legend is now reachable by tap, right-click, keyboard and
screen reader on every platform, and the organizer caption still carries the same
information unconditionally.

### 8.3 The Find-card stack (gap 5)

Not a bug — and now pinned rather than eyeballed. The enricher projects
`attendanceSummary` **only** when the viewer is PLAYING (`gameAttendance.service.ts`),
so a stranger's card can never grow a stack. The derivation moved out of
`GameCard.tsx` into `buildAttendanceRailData()` in `attendanceRailData.ts`,
which gives it a test seam: six cases cover the `null` cases, the four-face cap
with the real fraction, the per-face answer mapping and the nameless-user
fallback. `docs/domains/games.md` now states the rule, and UI test case
`GD-AT-28` walks it.

### 8.4 Integration-test teardown (gap 6)

`ChatSyncEvent` has no foreign key to `Game` (`contextId` is a plain string), so
nothing deleted the row the no-show system message writes. Teardown now removes
it explicitly, and every step goes through a `cleanup()` helper that **warns**
instead of swallowing: a teardown failure can no longer leave rows in
`padelpulse_dev` without anybody hearing about it. Verified by running the test
and then querying for orphaned `ChatSyncEvent` rows — zero.

### 8.5 The reminder filter is testable (gap 7)

The three lines inside the scheduler's private method became
`attendanceReminderRecipients(participants, hoursBeforeStart)` in
`attendanceRules.ts` (with `ATTENDANCE_SECOND_REMINDER_HOURS`, which the
scheduler now imports instead of declaring). `attendanceRules.test.ts` covers
the 24 h "everyone" pass, the 2 h filter, the boundary either side, that a
non-PLAYING recipient is never filtered on an answer it was never asked for,
that a fully answered roster gets nothing, and that the input array is not
mutated.

### 8.6 The iOS build caveat from §7.5

`BandejaWatchWidgets/NextGame/NextGameProvider.swift` guarded `WidgetRelevance`
with `@available(watchOS 11.0, *)`, but `RelevanceKit` also imports on iOS 18+,
so an iOS-SDK compile of that target failed before the rest of the workspace
built. The attribute now names both platforms. `xcodebuild -scheme App` for a
generic iOS Simulator destination **builds the whole workspace clean**.

### 8.7 Verified

| Check | Result |
|---|---|
| `Backend npm run build` / `npm run lint` | clean |
| `Frontend npm run build` / `npm run lint` | clean |
| `Backend npm run test:attendance` | 3 files ok |
| `Frontend npm run test:attendance` | 6 files, 61 tests |
| `gameAttendance.invariants.integration.test.ts` against `padelpulse_dev` | ok, and it leaves no orphaned `ChatSyncEvent` behind |
| `./gradlew :app:testDebugUnitTest --tests 'com.funified.bandeja.push.*'` | 4 classes, incl. 7 attendance cases |
| `xcodebuild -workspace App.xcworkspace -scheme App -destination 'generic/platform=iOS Simulator'` | **BUILD SUCCEEDED** |
| `ios/scripts/run-watch-unit-tests.sh` (watchOS simulator) | **TEST SUCCEEDED**, 81 cases, 0 failures |

Both native test runners need a non-default toolchain on a dev Mac: Android
gradle needs JDK 21 (`JAVA_HOME` → the Android Studio JBR), and the watch script
needs `WATCH_TEST_DESTINATION` when no simulator is literally named "Apple Watch".

### 8.8 Defects found in the re-verification pass

Re-reading the round-2 code found four real ones, all fixed and re-verified:

| Where | Defect | Fix |
|---|---|---|
| `ChatReplyMessagingService.java` | a payload with `nativeHandler: 'attendance_actions'` but incomplete tokens was **dropped entirely** — the branch returned after `show()` did nothing, so a missing token became a missing notification | `show()` returns a boolean and the branch falls through to the ordinary reminder. Covered by `anIncompletePayloadFallsBackToAnOrdinaryReminder` |
| `GameDetailViewModel.fetchGame()` (watch) | `attendance = try? await …` blanked the section on any transient refresh failure | only assigns on success, keeping what is already on screen |
| `GameDetailViewModel.answerAttendance()` (watch) | set `self.error`, which paints a red line in the **game's** error slot — contradicting its own doc comment and the silence the shade handlers keep | no longer sets `error`; the unchanged buttons are the message |
| `GameDetailsShell.tsx` | the legend sheet was mounted conditionally, off-convention and skipping vaul's enter animation | rendered unconditionally with `open`, like every other sheet in the file |

Also removed three dead computed properties (`canAnswerAttendance`,
`viewerAttendance`, `needsAttendanceAnswer`) that the watch view never read —
the view uses `vm.attendance` directly and the list row uses
`WatchGame.needsAttendanceAnswer`.

Payload budget checked rather than assumed: a Russian reminder with two real
469-byte tokens, both titles and both acknowledgements is **1506 bytes** of the
FCM 4096-byte data limit.

### 8.9 Still manual

Only what a simulator cannot prove: the shade buttons on a real device (§7.6)
and the watch flow on real hardware — `docs/UI_TEST_PLAN.md` §9.14b
(`GD-AT-60`…`GD-AT-67`) and §18.8b (`PN-AT-01b`…`PN-AT-02e`).
