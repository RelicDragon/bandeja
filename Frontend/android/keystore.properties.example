# Copy to keystore.properties (gitignored). Paths are relative to Frontend/android/app/.
storeFile=../../../bandeja-release.keystore
storePassword=your-store-password
keyAlias=your-key-alias
keyPassword=your-key-password
