/** Max recording length (3 min). */
export const MAX_VIDEO_DURATION_MS = 3 * 60 * 1000;

/** After client transcode. */
export const MAX_VIDEO_BYTES_AFTER_ENCODE = 50 * 1024 * 1024;

export const MAX_VIDEO_WIDTH = 1280;
export const MAX_VIDEO_HEIGHT = 720;

/** ~3 Mbps target. */
export const TARGET_VIDEO_BITRATE = 3_000_000;

export const SEND_VIDEO_UPLOAD_PHASE_MS = 180_000;
