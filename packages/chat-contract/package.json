{
  "name": "@bandeja/chat-contract",
  "version": "1.0.0",
  "description": "Shared chat sync event types and protocol constants",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "../../scripts/run-heavy npx -p typescript@6.0.3 tsc",
    "test": "../../scripts/run-heavy sh -c 'npm run build && node --test test/parity.test.mjs'"
  },
  "license": "ISC",
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
