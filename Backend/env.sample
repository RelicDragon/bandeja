PORT=3000
NODE_ENV=development

DB_HOST=localhost
DB_PORT=5432
DB_NAME=padelpulse_dev
DB_USER=postgres
DB_PASSWORD=postgres
DB_SCHEMA=padelpulse

DB_URL=postgresql://postgres:postgres@localhost:5432/padelpulse_dev?schema=padelpulse

JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=90d

# Telegram Bot Token (from @BotFather)
TELEGRAM_BOT_TOKEN=

# Media/Image base URL - can be local server or CDN
IMAGE_BASE_URL=http://localhost:3000

# Frontend URL for CORS (production: http://91.98.232.51 or your domain)
FRONTEND_URL=http://localhost:5173

# Firebase Cloud Messaging (FCM) for Android Push Notifications
# Get these from Firebase Console → Project Settings → Service Accounts
FCM_PROJECT_ID=bandeja-padel
FCM_PRIVATE_KEY=
FCM_CLIENT_EMAIL=

# OpenAI API Key for message translation service
OPENAI_API_KEY=

# Puppeteer Chrome executable path (optional)
# If Puppeteer's bundled Chrome fails due to missing system dependencies,
# set this to a system-installed Chrome/Chromium path (e.g., /usr/bin/chromium-browser)
PUPPETEER_EXECUTABLE_PATH=