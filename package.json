{
  "name": "padelpulse",
  "private": true,
  "engines": {
    "node": ">=24 <25",
    "npm": ">=11"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:contract": "npm run build -w @bandeja/chat-contract",
    "test:contract": "npm run test -w @bandeja/chat-contract"
  }
}
