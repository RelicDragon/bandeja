{
  "name": "padelpulse",
  "private": true,
  "engines": {
    "node": ">=24 <25",
    "npm": ">=11"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:contract": "./scripts/run-heavy npm run build -w @bandeja/chat-contract",
    "test:contract": "./scripts/run-heavy npm run test -w @bandeja/chat-contract",
    "build:unread-contract": "./scripts/run-heavy npm run build -w @bandeja/unread-contract",
    "test:unread-contract": "./scripts/run-heavy npm run test -w @bandeja/unread-contract"
  }
}
